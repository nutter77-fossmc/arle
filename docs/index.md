# Maintainer Doc Index

> **Looking for getting-started, install, or HTTP API docs?** Go to
> [README.md](../README.md), [docs/install.md](install.md),
> [docs/troubleshooting.md](troubleshooting.md), or
> [docs/http-api.md](http-api.md) instead. This file is for ARLE maintainers
> tracking canonical truth surfaces, active plans, and experience logs.

Last refreshed: 2026-05-10 EOD+2620 (**🎯🚫 BILATERAL A+B PATH READY + Machete KILLED + Priority table FINAL + SKILL v1.16.0 #43 graduated** — 18-tick chain since EOD+2150 sediments comprehensive forward path post-REFUTATION (-14.2% measured, NOT -35.3% extrapolated). Decisive findings: (1) `fc33cfb` Machete port KILLED — sm_90+ WGMMA+TMA only, 0% on sm_89 ARLE primary; (2) `bccf1bd` consistency audit — Hybrid plan -14% prediction was CORRECT, my §12.7 extrapolation was wrong; Hybrid is auxiliary that STACKS with Medusa; (3) `0a0d221` + `1ccb41f` + `f0c7561` Medusa Task #28 ready for codex pickup (~350 LOC Rust + 0 new CUDA kernels); (4) `e021026` Alpaca data already prepped (52,002 rows chat-format); (5) `b6b8adc` ARLE marlin_pf8/ = COMPLETE vLLM marlin/ fork (P2 dropped); (6) `f8b8174` + `b2cccb9` + `6024d2b` + `e60046b` P2.5 QQQ diff-port — full Step 1 audit reveals QQQ Hunk 8 IS the M'' schedule auto-tune; ARLE has REAL sm_89 advantages (PTX cache-hint cp_async + 10 compile configs vs QQQ's 4); refined effort 4.5-6.5 hr with mixed expected outcome distribution (20% gain / 30% neutral / 40% slight regression); (7) `494ad3a` Task #47 H1' v2 redesign brief — extend MarlinScratch ~50 LOC; (8) `6577ba6` SKILL v1.16.0 graduated #43 "always source-survey before pending-list" canonical (n=5 evidence, ~3-4 days saved). **FINAL priority table**: P1 A+B combined (4-5d, 2.61× tok/s + -14% latency, gated on user GO) > P2.5/M'' merged (4.5-6.5 hr, mixed) > P3 Task #47 v2 (1d parallel) > P4-P5 cutlass rewrites (low priority). Compound 5-6 days for: 2.61× tok/s + -14% latency + PF8 path unblock + P2.5 reference. 19-doc chain ready for next-session pickup. Codex IDLE 8+hr (Worked 26m 51s); user-blocked on direction. SKILL canonical: 38 anti-patterns. Pre-EOD+2150 retrospective preserved below.) (**🎯 BF16 baseline measured + W4A16 STRICTLY DOMINATES finding — quantization is strict win at sm_89 16GB, NOT tradeoff** — Per `eab166d`: BF16 conc=1 prompt=512 = TTFT 68.7ms/ITL 14.0ms/69.3 tok/s; W4A16 vs BF16 = TTFT -4%, ITL **-59%**, tok/s **+130%** with 0.0% accuracy diff. Decode is HBM-bound; 4× less weight memory at W4 → ~4× theoretical decode speedup. Validates W4 quant axis as primary path. **Cumulative session-tail Claude bench tally: 19 PASS + 1 NULL + 1 misapplied** (per `024cee2` summary §1.5 update). New rule: BF16 stays in correctness-check path only; routine perf benches use W4A16 as ceiling baseline. Pre-EOD+1990 retrospective preserved below.) (**🎯 N=5 HYBRID VALUE PROGRESSION FULLY MEASURED — W4A8 long-ctx 3-point curve (2k/4k/8k) parallel to W4A16 4-point + §9.4 prediction validated +4% margin + Hybrid Option B value progression: 512(-1.4%) → 2k(-7.5%) → 4k(-11.1%) → 8k(-14.2% MEASURED) → 32k+ ≈ -20% Machete-class (asymptotic)** — Since EOD+1860 below, 2 more substantive Claude benches: `8038cd3` W4A8 prompt=4096 = TTFT **409.4 ms (-29% vs W4A16, advantage holds)** + Hybrid value **-11.1%** + 1 cache demotion; `43ac26f` W4A8 prompt=8192 = TTFT **985.4 ms** (+4% over §9.4 prediction of 948, validated within margin) + Hybrid value **-14.2% MEASURED** (predicted -15.7%, within ±10%) + 3 cache demotions. **Strategic refinement**: hybrid value asymptotic, not linear — diminishing returns mean Machete-class -20% threshold needs 32k+ native context (Qwen3-4B's max) or 64k+ YARN-extended. **For "world-first 长序列推理引擎" goal**, Option B's ~2wk checkpoint format tooling pays off ONLY IF target audience workload distribution is 32k+ context-heavy. Decision now hinges purely on user's target context length distribution. Pre-EOD+1860 retrospective preserved below.) (**🎯 LONG-CONTEXT EVIDENCE BLOCK LANDED — n=4 W4A16 scaling curve (prompt=512/2048/4096/8192) + W4A8 long-ctx parallel + Phase 4 formula validated EXACT MATCH on 8k extrapolation + Hybrid Option B value re-evaluated for long-ctx workloads (-7.5% perceived latency at 2k vs -2.4% at short-ctx)** — Since EOD+1170 below, 5 substantive long-ctx benches: `a15a062` errors entry (default `--max-seq-len 4096` caps prompt at ~1997 tokens, all-rejected at prompt=2048; procedural rule: pass `--max-seq-len ≥ 2× prompt_tokens`); `2048eca` W4A16 prompt=2048 = TTFT **272.1 ms / ITL 6.4 ms / 117 tok/s / 0 failures** + Phase 4 formula validated (predicted 264 → actual 272, +3%); `4e2f39a` W4A16 prompt=4096 = TTFT **577.6 ms (8.75× super-linear)** + 1 prefix cache demotion event (memory pressure visible at 4k); `b340e2c` W4A16 prompt=8192 = TTFT **1335.5 ms (20.2× super-linear)** + 4 cache demotions + **EXACT MATCH on §10.3 prediction (1.2-1.4s)**; `ec879ca` W4A8 prompt=2048 = TTFT **191.3 ms (W4A8 -30% vs W4A16, advantage WIDENS at long ctx)** + ITL +97% (architectural) + 0 cache demotions (less mem pressure than W4A16). **Refined extrapolations**: 16k TTFT ≈ 3.0-3.5s, 32k native ≈ 7-9s, 64k YARN-extended ≈ 14-18s. **Hybrid Option B re-evaluation**: long-ctx grows hybrid value 3-5× (-7.5% at 2k vs -2.4% at conc=4 short-ctx) — Option B becomes more attractive than the prior short-ctx-only analysis suggested for "world-first 长序列推理引擎" target audience. Pre-EOD+1170 retrospective preserved below.) (**🚫🎯 PF8.5 KILL DEFINITIVELY LANDED + 4-arm A/B isolates PF8.3 substrate as bug + NEW W4A8 vs W4A16 vs INT8 perf comparison sets Medusa floor — broke "user-only" framing constraint via run_in_background; Claude-runs the bench user has been blocked on for 7+ ticks** — Since EOD+930 below, 8 Claude-run benches (~30 min wall-clock) + 7 substantive commits: `415de06` PF8.5 RUNS (deviation from 'user-only' — subprocess sleep ≠ Claude tool sleep, run_in_background bypasses cleanly); `0be278f` SUBSTRATE-KILL verdict (5878 `gemm_w4_fp8_marlin_cuda code 2` failures from Pass 3 warmup B=1, 5385 'successful' = broken signal artifact); `a7f913b` pickup queue PIVOT (P1: #47 H1' → #28 Medusa); `7ed8160` Arm B `INFER_PREFILL_WARMUP=0` REFUTES warmup-DEPENDENT framing (5959 failures still occur with warmup off — per-call alloc IS the bug, warmup just surfaces it first); `06b7437` Arm C W4A16 control HEALTHY (0 failures, TTFT 66.0ms / 5.8ms ITL / 799 tok/s — definitively isolates PF8.3 not infer-binary); `d8b2870` Arm D W4A8-zpfix control HEALTHY (Task #48 default works at conc=1 sustained: 54.2ms TTFT / 11.9ms ITL / 409 tok/s) + **NEW perf comparison**: W4A8 wins prefill (-18% TTFT vs W4A16) loses decode (+105% ITL); W4A16 vs INT8: +23% TTFT, ITL similar; PF8 was supposed to bridge gap; `430a4be` SKILL Phase 5 candidate "twin-control-arm discipline" (n=1 evidence, 24:1 ROI: 10 min wall-clock prevents ~4 hr misdirected codex pickup). **SKILL #29 to n=6** with `7ed8160` self-correction of warmup framing — single-source artifact inference decay caught within 30 min by follow-up A/B. **Medusa Phase 1.A perf floor**: TTFT ≤ 54.2ms / ITL ≤ 5.8ms / tok/s ≥ 799 / acceptance ≥ 70%. Task #44 PF8 chain CLOSED-KILL (3-arm + 1 control evidence ironclad); Task #47 H1' BLOCKED with broader scope (per-call alloc redesign, not warmup interaction); Task #28 Medusa P1 unblocked. Pre-EOD+930 retrospective preserved below.) (**🎯 Cooperative loop saturation tail — Task #39 housekeeping close + Task #43 Arm A artifact deep-dive (70:1 OOM ratio) + 30-min self-correction (16× TTFT degradation hidden behind 'server survived' framing) — SKILL #29 to n=5** — Since EOD+860 below, 3 Claude commits all under codex idle ~90+min: `45ec4f4` Task #39 M_rope-yarn-scaling impl marked completed (substrate fully landed Phase 1+2+3a per `37ae5f9` final-consolidation; Phase 3b PPL eval blocked on hardware OOM not impl scope); `da7f5a2` Task #43 Arm A artifact deep-dive via on-disk parse — quantified **70:1 OOM ratio** (Arm A scratch=ON 70 OOMs / 32 y_fp16 + 9 x_fp16 marlin scratch / Arm B scratch=OFF 1) that codex `83fc5d0` INVERSE finding hadn't captured; `d09623a` self-correction on da7f5a2 — bench CSV parse revealed Arm A is **16× slower TTFT (1502ms vs 94ms)** + **26× lower throughput (1.23 vs 32.4 tok/s)** than Arm B; 'server survived' framing was misleading; **SKILL #29 to n=5** with Claude self-application of single-source artifact framing; SKILL #38 framing corrected to 'graceful-not-panic' not 'load-bearing for survival'; **Task #47 H1' design now has 2 A/B gates** (OOM-regression per da7f5a2 + TTFT/tok-s regression per d09623a); new procedural rule: parse bench CSVs before strong claims like 'server survived' / 'fix worked'. Pre-EOD+860 retrospective preserved below.) (**🎯 Cooperative loop SATURATED — 3 task closures + 2 SKILL graduations + session retrospective committed; awaiting user direction** — Since EOD+820 below, additional milestones: Task #48 LANDED via codex commit `8d1caad test(cuda): use qzeros-fixed W4A8 fixture for accuracy gate` (3 files +61/-7, both `e2e.rs:21` + `greedy_consistency.rs:30` fixed per Claude `be133f8` audit, Worked 26m 51s, full verification PASS); pickup matrix updated `66d7b74` (P0 #43 + P2 #48 CLOSED); session retrospective `53a23e2` (175 LOC, comprehensive metrics: ~3.5h session-tail, 55+ Claude commits, 3 codex commits, 36 canonical anti-patterns, 6+ candidates, 4 cooperative dispatches, 2 SOLID self-corrections, full §1-§7 structure for next-session pickup). Cooperative loop SATURATED — codex idle since ~10:55 KST (6+ idle ticks), user firing /loop manually (engaged but PF8.5 license decision pending — multiple PushNotifications dispatched without user response). All forward paths blocked on user action (`bash scripts/pf85_bench_v11_user.sh` for PF8.5 LICENSE/KILL branch, OR new direction for standalone Medusa / Task #30 dispatch). Pre-EOD+820 retrospective preserved below.) (**🎯 Multi-task cooperative loop COMPLETE — Task #35 LANDED + Task #43 DISPROVEN INVERSE + Task #48 BREAKTHROUGH eb2b4b6-fixture + SKILL v1.14.0 (#36 + #38 graduated) + 50+ Claude commits chain + 6+ SKILL candidates accumulated** — Since EOD+750 below, key milestones: Task #43 dispatched + DISPROVEN-INVERSE (Arm A env=on KILL with 36 OOM / Arm B env=off HEALTHY per codex `83fc5d0` + Claude self-correction `e8b6b31` — my dispatch-audit hypothesis was directionally wrong, n=2 evidence for SKILL #36); SKILL v1.14.0 graduated #36 (static code audit hypothesis-grade, behavioral A/B ground truth — both required) per `d2c987f` + frontmatter version fix `62e8295`; Task #48 W4A8 84.4% regression dispatched + codex BREAKTHROUGH at min ~13: existing `eb2b4b6` research entry documents default `Qwen3-4B-W4A8-marlin` is naive checkpoint producing 100% garbage (validates SKILL #29 strongly, n=2); Claude be133f8 audit found SAME broken default in BOTH `e2e.rs:21` AND `greedy_consistency.rs:30` — codex picked up audit recommendation, modifying both test files (cooperative pattern win, ~10:48 KST per `154bb81`); codex Task #48 wrap commit pending (~5 min); workspace recovery from 116-commit reset to remote 3844c84 (workspace was inadvertently reverted), pulled back + cherry-picked detached-HEAD commit (5ec37cc → 62e8295) + later e5deac8 → 01bcefa via temp-branch-recovery pattern documented at `4a2a347`. Cooperative loop pattern fully validated end-to-end with **2 successful codex dispatches** following Task #35 LANDED (Task #43 + Task #48). SKILL canonical anti-patterns: 28-34 + 36 + 38 = 36 total. Single-evidence candidates remaining: #35 (n=2 evidence reachable via Task #48) + #37 + #39 + #40 + #41 (terminal silence ≠ no progress) + #42 (temp-branch recovery for detached-HEAD during peer agent's git checkout) + #29 enhancement (broken defaults duplicated via copy-paste constants). PF8.5 license decision STILL blocked on USER-runs-only `bash scripts/pf85_bench_v11_user.sh`. Pre-EOD+750 retrospective preserved below.) (**🎯 Cooperative loop COMPLETE — Task #35 LANDED (a2ad788) + SKILL v1.13.0 #38 GRADUATED + Task #43 dispatch in flight + 32+ Claude commits chain + 5 SKILL candidates accumulated** — Since EOD+660 baseline below, Claude-authored chain since `f36d176`: `b4a3c38`+`60f114f`+ progression docs → `e6b4197` Task #35 §6.12 W4 startup MEASURED 8.2s → `3beab7f` §6.11 codex applying SKILL #29 to OWN prior data → `940c7cc` Task #35 PRE-bench prediction + `5f3f58f` reconciliation + `2d00de3` SKILL candidate #37 multi-shape bench + `05ce3cb` 3-bug catch reconciliation + `2cc608a` H1' design REVISION (~70 LOC, MarlinScratch already exists, candidate #36) + `1ba06f0` Task #43 hypothesis STRONGLY EVIDENCED (linear.rs:2064-2095 dispatch audit) + `940c7cc`/`ead46dc` `pf85_bench_v11_user.sh` user-runnable PF8.5 license (1 command) + `63769be` Medusa Alpaca cross-link (HF auth blocker bypass) + `458394c` `task43_hypothesis_test.sh` + `f63838b` codex-pickup-queue-2026-05-10 (4 ready pickups + dispatch logic) + `182d67b`+`cd57410` Task #35 §6.13 OOM-fallback + final RECONCILED → **`a2ad788` codex Task #35 LANDED** (perf(scheduler): prewarm prefill paths at startup, 7 files +343, +8186ms n=3 stable Pass 3 W4 startup overhead, B=8 OOM fallback to 1024 tokens/row, 4 substantial issues caught + fixed via codex review + re-verification) → **`8b530ad` SKILL kernel-optimization v1.13.0 GRADUATED #38** (warmup target shape budget must clamp to effective workload × hardware headroom, n=2 evidence reached: max_seq_len/chunked_prefill_size + B=8 2048 OOM) → `64d9b65` codex idle 25min after Task #35 → Claude tmux nudge dispatched P0 Task #43 → `cb86836` verdict implications pre-computed → `87dc965` Arm A INTERIM TOOL-QUIRK (no substrate KILL, validates SKILL #34b discrimination), Arm B running. **SKILL v1.13.0 canonical: 28-34 + 38 (35 total).** Single-evidence candidates remaining: #35 root-cause-TBD canary + #36 grep variants before designing + #37 multi-shape bench discipline + #39 post-fix bench data stale. Codex Task #43 ~6min, GPU 15.8GiB/100% Arm B in flight (~15-25 min to verdict). PF8.5 license decision STILL blocked on USER-runs-only `bash scripts/pf85_bench_v11_user.sh` (zero-friction one-command path). All 4 future codex pickups scaffolded as runnable scripts/recipes. Pre-EOD+660 retrospective preserved below.) (**🎯 Cooperative loop tail — 18 Claude commits + codex Task #35 in flight + H1' DESIGN REVISION + Task #43 hypothesis CONFIRMED + SKILL #33 n=2 evidence + 3 SKILL candidates documented** — Since EOD+580 baseline below, Claude-authored chain since `40a9184`: `9c9d699` framing-trap rule-6 case study (PF8.3 narrow vs wider framing parallel to 2026-05-08 NVTX trap) → `868e147` `pf83_bench_health.sh` (operationalizes #34b) → `e3e1ab5` W4A8-vs-BF16 84.4% regression flagged Task #48 + skill candidate #35 (root-cause-TBD canary) → `60f114f` matched-control escape-hatch discipline evidence (codex Task #35 `INFER_PREFILL_WARMUP=0`, candidate for future SKILL revision) → `20adfb3` pickup-state §3 POST-COOPERATIVE-LOOP addendum → `fde71bf` bench IN FLIGHT update → `7004578` bench-and-trace-spec §7.6 (cross-link #34b into canonical spec) → `2cc608a` H1' design REVISION (~70 LOC not 110 — `MarlinScratch` ALREADY EXISTS in `linear.rs:317-323`, PF8 only missing variant) + skill candidate #36 (grep variants before designing from scratch) → `1ba06f0` Task #43 W4A16 sustained-load HYPOTHESIS STRONGLY EVIDENCED via dispatch audit (`linear.rs:2064-2095` falls back to per-call alloc when `marlin_scratch=None`; gated on `INFER_PREFILL_GRAPH=1` per `qwen3/forward.rs:312-313` per `35fc3cf`; if Task #43 bench was run WITHOUT this env → ~580 GB cudarc churn = same fragmentation as PF8.3 KILL = **two-tasks-one-PR opportunity**) → `940c7cc` Task #35 Pass 3 cap=8 first-burst formula prediction PRE-bench (TTFT p99 -5/-20%, first-burst -30/-60%, ITL ≈ 0, +282.7ms startup) anchored to Task #31 baseline → `ead46dc` `pf85_bench_v11_user.sh` (single-shot user-runnable PF8.5 conc=1 license bench, integrates pre-flight + cleanup + guidellm + health check + license verdict) → `5f3f58f` Task #35 prediction §6 RECONCILED (sustained-load held within ±2.5% noise, server startup +282.7ms exact, but first-burst NOT MEASURED → license REVIEW deferred; codex Task #35 §Rule independently validated falsification §4.1) → `2d00de3` SKILL candidate #37 multi-shape bench discipline (codex's wins entry §Rule sedimented as future skill candidate) → `05ce3cb` CRITICAL CAVEAT: codex review caught **3 substantial bugs** in Task #35 (sync() double-call + chunked_prefill_size derivation + temporary-context resource leak — Pass 3 was effectively NO-OP for graph-prefill case in measured bench!); SKILL #33 n=2 evidence symmetric magnitude (PF8.3 ace3cbe = 3 bugs / Task #35 = 3 bugs / both substrate / both formal gates PASS / strong "battle-tested" canonical signal). Codex Task #35 ~48min: applied review fixes, incremental rebuild PASS 7.43s, targeted greedy_consistency RE-running with new larger Pass 3 shape; will re-bench post-fix then commit. PF8.5 license decision STILL blocked on USER-runs-only `bash scripts/pf85_bench_v11_user.sh`. Pre-EOD+580 retrospective preserved below.) (**🎯 PF8.3 H8 DISPROVEN, KILL is LOAD-DEPENDENT, H1' design landed** — verify script `de314d2` post-`81672c3` H8 diagnostic build: kernel WORKS at conc=1 single requests (valid Chinese+English completions), diagnostic NEVER fired (no sticky cudaGetLastError). H8 DISPROVEN per `57c37b5`. PF8.3 KILL signal from `0cde63d` is LOAD-DEPENDENT — kernel functional at conc=1, allocator fragments at conc≥2 sustained (~9 MB × 252 ops × N conc × 60s = ~580 GB cumulative cudarc churn → CachingAllocator pool fragmentation → cudaErrorMemoryAllocation code 2). H1' static-scratch refactor design `05e2135` (`docs/plans/M_pf83_h1prime_static_scratch.md` 264 LOC) **codex-pickup-ready**: PF8Scratch struct + State integration + 110 LOC delta + 10-step checklist; gated on user-run bench v11 license decision (Claude session sleep limits prevent automation, full invocation in pickup state §3 + DISPROVEN doc §3). **🛠️ skill kernel-optimization v1.12.0 LANDED `0be7220`** — anti-pattern #33 (codex review value-add IS load-bearing for non-trivial substrate, NOT formality — caught 3 real bugs/27min review on `ace3cbe` PF8.3 substrate that build+clippy+greedy+e2e all PASSED) + #34 (greedy_consistency single-request PASS NECESSARY but NOT SUFFICIENT for new GEMM substrate; pair with sustained-load bench at conc 1+2+4) + #34b (bench reports 0 successful → CHECK SERVER LOG FIRST before debugging tool, 30+min v3-v10 wasted on guidellm CLI quirks when real cause was kernel 100% failure visible in /tmp/<server>.log). Task #46 H8 fix CLOSED; Task #47 H1' refactor CREATED (codex-pickup-ready, bench v11 gated). Codex this session-tail on Task #35 cap=8 prefill warmup (Pass 2→Pass 3 rename + `reset_for_warmup_clear` State trait + 3 impls per `58b0ac1` reconciliation; Working ~7min: 5 files modified — model.rs + qwen3/qwen35 forward + deepseek/state + scheduler/cuda/core/warmup, debug build PASS, CUDA release+clippy+greedy_consistency in flight). Pre-H8-verdict context preserved below: **🚫 PF8.3 RUNTIME KILL** `0cde63d` — substrate LANDED `11763ba` (12 files +3936/-13 LOC, codex commit) but `gemm_w4_fp8_marlin_cuda` fails 100% with code 2 cudaErrorMemoryAllocation under sustained load (101380/101380 requests across 10min). greedy_consistency PASS at conc=1 was false-positive; sustained-load bench reveals the runtime bug (skill v1.12.0+ #34 candidate: greedy single-request NECESSARY but NOT SUFFICIENT for new GEMM kernel). 8 hypotheses ranked, **H8 HIGHEST** per `c9abe8e` wrapper re-read: line 250-256 `cudaGetLastError()` returns LAST sticky error from ANY prior CUDA call, may surface unrelated prior-kernel error as gemm wrapper return. **Task #46 H8 fix path ready** for codex pickup (`1b3f76c` plan + ~40 min effort: 1-line cudaGetLastError clear at line 138 + 3 fix options + verify with greedy_consistency at conc=2,4 per skill #29 strengthening). **🚫 Medusa Phase 1.A pivot BLOCKED on HF auth** (`ad14636`): HF_TOKEN not set + `lmsys-chat-1m` is gated dataset (license accept + token required). 5 user options pending: (1) HF auth setup → Medusa (2) non-gated alt dataset (humaneval) (3) PF8.3 H8 fix per Task #46 (4) #35 cap=8 prefill warmup (M_warmup directive reconciled per `58b0ac1`: Pass 2 naming collision + reset_for_warmup_clear gap) (5) session pause+summary. PF8.3 substrate stays in tree, dispatch opt-in (default off per `db063ff`). PF8.5 prep tooling COMPLETE (`3fa5e74`+`84d61eb`+`c382fba`+`bf47413`+`e99e5a5`+`a6cf5ac`+`9bb3843`+`c6ccd24`+`172c311`+`45579c0`) but UNUSED until kernel fix lands. Real BASELINE INT8 numbers captured at conc 1-4 in v3 partial (53.6/68.4/110.2 ms TTFT mdn, 6.8/7.4/8.3ms ITL, 697/1259/2248 tok/s); treatment FP8 NEVER MEASURED (every bench attempt v3-v10 hit 0 successful PF8 requests, then 30+ min was wasted on guidellm CLI quirks debugging until server log direct verify revealed the kernel KILL — skill v1.12.0+ #34b candidate: bench reports 0-successful-req → CHECK SERVER LOG FIRST). Server orphan PID 1942070 killed, GPU back to baseline 1293 MiB / 0%. ~80 commits this session, 9+ hours bilateral cooperative pattern. Original PF8.1+PF8.2 progress preserved below for retrospective: **🎯🔧 PF8 chain Claude-driven progress**: PF8.1 ✓ landed `940f49e` (BF16→FP8 e4m3 quant kernel, 87 LOC, sm_89 native intrinsic) + runtime SMOKE PASS `b628eca` (max rel err 5.99% < 12.5% FP8 floor, 0 outliers/32768); PF8.2 ✓ landed `940f49e` (INT4 weight preprocess Apache 2.0 port, 70 LOC) + runtime SMOKE PASS `451d094` + **5th Claude hallucination caught BY smoke itself** (bit-pack arithmetic `0x76543210→0xFEDCBA98` was wrong, actual `→0x89ABCDEF` LSB→MSB); PF8.3 Strategy A' VALIDATED via codex codegen (`077b600` compile smoke PASS + `a0758e7` Strategy A' validation: vLLM `generate_kernels.py` produces single specialization `sm89_kernel_fe4m3fn_u4b8_bfloat16.cu`, 106 LOC + ~3300 LOC vendored marlin_pf8/ headers + 255 LOC ARLE-authored wrapper `marlin_w4_fp8_kernel.cu`); SUPERSEDES my earlier Path A (`818b4e0` ~400-600 LOC manual mma mirror) AND Path B (`259277c` k=32 manual ~800-1200 LOC). ARLE Phase 1 substrate `marlin_dequant.cuh:93-106` ScalarTypeTag aligned with vllm IDs (kU4B8=2, kFE4M3fn=5) eliminated worst-case scalar_type.hpp port. **COMPILE+CHECK+CLIPPY+greedy_consistency on hybrid checkpoint `infer/models/Qwen3-4B-W4-hybrid-zpfix` (4.5GB, quant_type=marlin_w4_hybrid) ALL PASS** — codex commit imminent (Working ~24m on `codex review --uncommitted` final pass with 900s timeout). FFI symbol `gemm_w4_fp8_marlin_cuda` mirrors W4A8 pattern (gemm.rs:64). PF8.4 dispatch wiring ✓ LANDED `db063ff` (38 LOC, MarlinW4FP8Prefill enum + INFER_MARLIN_W4_FP8_PREFILL env var); PF8.5 prep tooling COMPLETE: `3fa5e74` eval_ppl_pf83.py (PPL Δ% gate) + `84d61eb` bench_pf83_ab.sh (e2e A/B wrapper) + `c382fba` pf83_license_sequence.sh (orchestrator with --skip flags + KILL exit) + `e99e5a5` defaults to hybrid checkpoint per `473081d` anti-pattern #29 follow-up; license gates per `aebd4a5`: TTFT Δ ≥ -8% σ<5% n=3 + greedy PASS + PPL Δ% ≤ +1.0% wikitext; **🛠️ skill kernel-optimization v1.11.0 LANDED `b551bea`** canonicalized 4 anti-patterns from session retrospective de36538 — #29 default test fixtures may be known-broken (load-bearing this session: codex caught greedy_consistency PASS on W4A8 keeping PF8 INACTIVE before re-running on hybrid checkpoint, saved false-license risk), #30 git status BEFORE commit not just before add, #31 ANY ARLE surface claim needs raw evidence in SAME response, #32 peer Waiting >5min warrants direct ps/log/curl verify; **🚫 Machete framing CANONICAL DISAMBIGUATION `aa9f72e`** — 5+ user reissuances of literal "Machete W4 移植" formally mapped to Path B-Phase2' (PF8 chain Strategy A') per `e65a096` Hopper-only blocker, future ticks won't re-litigate; 7 hallucinations sedimented this session (#1-#5 in de36538/b551bea + #6 `818b4e0` FP8 mma single-shape claim + #7 `473081d` claimed hybrid checkpoint missing while citing CARGO_MANIFEST_DIR convention that resolves to `infer/models/`) — pattern: even "deterministic computation" + "cited evidence" can be hallucinated, empirical smoke + raw cross-check are self-correcting; **🎯 Path B Phase 1 Substep 1.1 LICENSED `f86d0fd`+`4f1b036`** σ-tight n=2 Δ -3.2% ITL (matches e59beb5 conservative -3-8% estimate); **#34 RESOLVED `df37a68`** arle model download <id> CLI surface (~50 LOC, both feature gates clean) unblocks P0 #28 spec decode hypothesis path; **🎯 Path B Phase 1 Substep 1.1 LANDED via accidental bundle `09ae5a5`** — codex's `marlin_dequant.cuh` (651 LOC, hybrid strategy single-file + verbatim `namespace vllm` shim) + marlin_kernel.cu integration -16 net LOC; full PASS verification (cargo build 4m 43s, fmt, clippy `-D warnings` 3m 47s, greedy_consistency `test_greedy_solo_vs_concurrent` on Qwen3-4B-GPTQ-W4A16-marlin-zpfix 10.83s); NO throughput license claimed (correctness substrate only); follow-up `0d63a52` errors entry sediments cooperative-discipline violation (my `git add` + `git commit` race-bundled codex's WIP — skill v1.10.0+ anti-pattern #30 candidate "git status BEFORE commit not just before add"); `994a294` build-restore for `.h → .cuh` rename include consistency. **🚫 Substep 1.2 atomic_add KILLED in design** — raw grep proves W4A16 marlin_kernel.cu has only `int* locks` (no `max_par × 64 × n` reduce buffer to eliminate); W4A8 alt has buffer at `marlin_w4a8_kernel.cu:258` but TTFT axis better served by NEW prefill-only FP8 directive (P1 in revised priority); **🚫🔬 Path B-Phase2' Phase 0 P0.A KILLED `67f18b9` + architectural synthesis `61c9666`** — codex cutlass DIRECT FP8 GEMM smoke (post Claude `d5a6679` sm_89 template unstick: `GemmUniversalWithAbsMax` + `arch::Sm89` + `LinearCombinationGenericWithScalingAndAbsMax` epilogue) ran with `Status::Success` on Qwen3-4B linear shapes. **Decode (M=1 N=4096 K=2560)**: BF16 0.427ms TFLOPS=0.05 vs FP8-fast 0.229ms TFLOPS=0.09 = **1.86× speedup BELOW 2× kill threshold** = decode KILL. **Prefill (M=2048 N=4096 K=2560)**: 5.21× speedup BUT only 159.8 TFLOPS = 22.6% of 706 theoretical (codex flagged below absolute 50% gate); separate TTFT axis. **ARCHITECTURAL INSIGHT**: W4 decode HBM-bound on weight read (already 4× smaller than BF16); FP8 mma helps compute not bandwidth; activation is 0.2% of memory traffic in W4 GEMM → user's "-20-40% ITL via FP8 path" **structurally infeasible** on sm_89 W4 decode; same memory-bound ceiling explains why Machete (sm_90) wouldn't help on sm_89 even if backportable; PushNotification dispatched with revised priority: P0=#28 spec decoding (-50%+ ITL via amortized weight read, blocked on #34 HF Hub), P1=W3/W2 quant research, P2=Phase 1 dequant.h port (-3-8% ITL fallback, e59beb5), P3=NEW prefill-only FP8 directive (~700 LOC, -8-16% TTFT separate from ITL); P0.B PPL gate skipped per 6a6114d decision matrix; Task #41 completed; **🚫 #36 PrefixAwareAdmission Layer 2 KILLED `9bbc441`** Claude self-driven A/B via `scripts/bench_36_warmmix_direct.py` (3b4c89b + b96a1e7 /v1/stats parse fix): warm p95 +17%, **cold p95 +114%**, throughput -3.4%, starvation ratio 4.56× → **8.33×** at cold_soft_cap=3 + 8 slots + c=8. Substrate works (gate fires 73×, prefix_hit_rate=57.2%) but op-point makes every metric regress; `QueueBound` stays default at types.rs:214, `--admission-policy prefix-aware` retained as opt-in CLI. Three follow-up paths documented (cold_headroom sweep / session_id workload / c=32) — none P0 since #40 Path B.2 already delivered single-stream gap closure; **🛠️ skill kernel-optimization v1.10.0 `b96a1e7`** added anti-pattern #28 (hallucinated tool output overrides peer-agent investigation) from `ee2c5b0` SOLID-critical chain — Claude's prior `0f4d0ae` "correction" of codex on `--max-waiting-requests` flag existence was based on fabricated grep evidence; codex was correct from the start; sediment rule: when correcting peer file-content claim MUST re-run verification + quote raw output literally; **🚫 Machete sm_89 BLOCKER reaffirmed `e65a096`** 5-pt convergent evidence (collective_builder + mainloop + generate.py + Readme + 2026-05-09 prior survey all confirm Hopper-only) — default Path B-Phase2' (3e83741: W4+FP8 sm_89 native = real -20-40% ITL mechanism) absent explicit user "Path A confirmed" ack; **🔍 #36 P0 survey `5e902da` finds substrate FULLY WIRED** — all M_b3 steps LANDED (Step 1 `7c8fd61` admission_allows signature + Step 2 `prefix_aware_admission_allows_plan` real-signal pipeline + fail-open anti-starvation + Step 3 `--admission-policy` CLI flag at main.rs:124); **🛠️ skill kernel-optimization v1.9.0 `08d9b7e`** added anti-patterns #26 (smoke-shape ≠ production-shape capture-key cardinality from `a7a8b94` Path B v1 KILL) + #27 (bucketing without scalar-capture sync = semantic miss from `a56b7a9` Path B.2 win) from #37/#40 chain; **🚫 #33 KV W4A8 KILLED `ddf0615`** scalar inline INT4 unpack 1.12× < 1.5× kill gate; **🎉🎉 #40 Path B.2 TIER 1 STRONG PROCEED LANDED `c44788f`** — Engine TTFT p50 **2000ms → 150ms = -92.5%** + 388 unique keys → **7 unique = 98% reduction** + 98.5% LRU dominant key reuse + **+632% throughput** in matched-control 60s window;codex's "second-order bucketing" insight(captured scalar launch parameters use bucket capacity)load-bearing for win;**closes 4k/c=4 SGLang +76.6% gap as P0 outcome**;guidellm 0.6.0 client-side TTFT broken `e8d82b0` — bench tool bug isolated,server-side ground truth stands; **🎉 #24 W4A8 prefill graph capture hoist LANDED `35fc3cf`** opt-in via `INFER_PREFILL_GRAPH=1` + `INFER_HYBRID_W4A8_PREFILL=1`; **🚫 #37 Path A multi-key throughput KILLED `e462c53`** + **🚫 Path B v1 KILLED `a7a8b94`** Tier 4 100% miss at 4k production,Path B.2 bucketing(`a56b7a9`)recovers via 64-entry/128-row bucket dimensions; **🎉 M_rope-yarn-scaling Phase 1+2 LANDED `e30bffe..da53d81`** YARN/Linear/NtkAware scaling support across qwen3-spec + qwen35-spec + `precompute_rope_with_scaling` weight_loader integration,7 atomic commits + 51 unit tests + 1 wins consolidation `11fca7a`; **🚧 #26 M_xgrammar Phase 1 FFI scaffold WIP** codex-side `crates/xgrammar-sys` C++ shim + Rust safe wrapper(1046 LOC,upstream `mlc-ai/xgrammar` v0.1.34,no HTTP/scheduler/sampler integration yet); **🚫 P1.6/P1.4/P1.3 各 KILL** prior axis(prior log保留)。Phase 3 long-ctx YARN bench plan ready `8466202`(Qwen3-4B 64k YARN×2 / 128k YARN×4+FP8 KV CUDA-side viable,无需 Mac,native ctx=40960 confirmed)。Pre-built post-commit chain ready:`scripts/{validate_p24_phase0v3,setup_qwen3_yarn_config,post_p24_commit_pipeline}.sh`。Anchor:[`docs/experience/wins/2026-05-10-m-rope-yarn-scaling-phase1-phase2-landed.md`](experience/wins/2026-05-10-m-rope-yarn-scaling-phase1-phase2-landed.md),[`docs/research/2026-05-10-37-rescope-post-codex-multikey-impl.md`](research/2026-05-10-37-rescope-post-codex-multikey-impl.md). Priority order canonical in [`ROADMAP.md` §Next-Model Priority Order](../ROADMAP.md#next-model-priority-order). Pickup queue: [`docs/plans/codex-pickup-queue-2026-05-09.md`](plans/codex-pickup-queue-2026-05-09.md).

## Canonical Truth Surfaces

| Concern | Canonical source | Notes |
| --- | --- | --- |
| Support status of backends / APIs / model families / quantization | [support-matrix.md](support-matrix.md) | README and roadmap summarize only. |
| Stability levels and compatibility posture | [stability-policy.md](stability-policy.md) | Do not redefine tiers elsewhere. |
| Workspace topology and module entry points | [codebase-map.md](codebase-map.md) | Source of truth for "what exists today". |
| Architecture ownership and boundaries | [architecture.md](architecture.md) | `infer` owns runtime truth. |
| Benchmark and trace process | [bench-and-trace-spec.md](bench-and-trace-spec.md) | `guidellm` is the canonical e2e benchmark path. |
| Canonical e2e bench tool + parameter set | [plans/guidellm-integration.md](plans/guidellm-integration.md) | Wrapper script `scripts/bench_guidellm.sh` uses these params verbatim. |
| Contributor operating contract | [../AGENTS.md](../AGENTS.md) | Use with the canonical docs above. |

## Current Positioning

`ARLE` is a runtime-first Rust workspace.

- `infer` is the primary serving/runtime surface.
- `arle` is the unified local front door for agent, train, eval, and data
  workflows built on that runtime.
- Train/RL work is strategic because it strengthens the runtime loop; it does
  not create a second equal project identity.

If a plan or project note disagrees with that framing and is not explicitly
marked as the current source of truth, treat it as historical context.

## Active Projects

| Path | Status | Use this when |
| --- | --- | --- |
| [projects/2026-04-30-longctx-32k-128k-leadership.md](projects/2026-04-30-longctx-32k-128k-leadership.md) | Active — P0 mission | The question is the 32k–128k longctx world-#1 mission (4 phase plan, baseline panel, hardware tiers, current Phase 1 SGLang-row close + Phase 2 plumbing/regression status in §13/§13.A). |
| [projects/2026-05-02-agent-load-mission-expansion.md](projects/2026-05-02-agent-load-mission-expansion.md) | Active — mission expansion | The question is the agent-load world-#1 expansion: W3 short-prompt multi-turn, W4 tool-call resume, session affinity, prefix-cache reuse, and four-engine baseline gates. |
| [projects/2026-05-01-multi-gpu-f0-readiness.md](projects/2026-05-01-multi-gpu-f0-readiness.md) | Active | The question is single-node multi-GPU F0 readiness, scaffolded TP/PP/EP axes, NCCL smoke, and the gap matrix to real multi-rank serving. |
| [projects/2026-05-01-deepseek-v4-readiness.md](projects/2026-05-01-deepseek-v4-readiness.md) | Active — parallel product line | The question is DeepSeek V4 readiness, the DS0–DS8 gap matrix, and what `crates/deepseek-spec/` already scaffolds. |
| [projects/2026-05-01-spec-decode-integration-design.md](projects/2026-05-01-spec-decode-integration-design.md) | Active | The question is how Phase 2 spec decode plumbing integrates with the CUDA scheduler, verifier, and external draft state. |
| [projects/tiered-kv-cache.md](projects/tiered-kv-cache.md) | Active | The question is current KV-tier scope, milestones, or operator-facing status. |
| [projects/tiered-kv-runtime-flow.md](projects/tiered-kv-runtime-flow.md) | Active | The question is how scheduler, RadixCache, and tier coordinator interact at runtime. |
| [projects/mlx-backend-roadmap.md](projects/mlx-backend-roadmap.md) | Active | The question is Metal serving closure, MLX runtime direction, or the Qwen3.5 GGUF decode hot path. |
| [projects/agent-rl-self-evolving.md](projects/agent-rl-self-evolving.md) | Active | The question is how train/RL/self-evolution work strengthens the runtime spine. |
| [projects/agent-first-architecture.md](projects/agent-first-architecture.md) | Active but secondary | The question is long-horizon agent-serving priorities outside the current KV plan. |

## Active Plans

| Path | Status | Use this when |
| --- | --- | --- |
| [plans/2026-04-28-single-node-multi-gpu.md](plans/2026-04-28-single-node-multi-gpu.md) | Active | The question is the single-node multi-GPU plan (F0–F8 phases) for TP/PP/EP scaffolding and forward collectives. |
| [plans/2026-04-28-multi-gpu-f0-verification.md](plans/2026-04-28-multi-gpu-f0-verification.md) | Active | The question is the F0 verification protocol (NCCL link, rendezvous, all-reduce smoke, single-rank no-regression gate). |
| [plans/2026-05-01-longctx-spec-decode-phase2.md](plans/2026-05-01-longctx-spec-decode-phase2.md) | Active | The question is Phase 2 long-context speculative decode integration on top of the closed Phase 1 W1 c=4 SGLang row. |
| [plans/2026-05-01-mla-kernel-design.md](plans/2026-05-01-mla-kernel-design.md) | Design only | The question is the DeepSeek-family MLA CUDA kernel design (DS3) — formula, cache layout, prefill/decode dispatch. |
| [plans/2026-05-02-agent-load-bench-spec.md](plans/2026-05-02-agent-load-bench-spec.md) | Active | The question is the W3/W4 agent-load benchmark contract: short-prompt multi-turn, tool-call resume, session affinity, cache metrics, and four-engine baseline evidence. |
| [plans/2026-05-03-a8-gpu-sm-kv-io-kernel.md](plans/2026-05-03-a8-gpu-sm-kv-io-kernel.md) | Pending — 待落地 (gated on (A) closing W4) | The question is whether to swap `cudaMemcpyAsync` for an SM-driven kernel on T0↔T1 paged-block transfers (LMSYS 3× claim). Read before touching `kv_tier/transport`. |
| [plans/infer-observability-v1.md](plans/infer-observability-v1.md) | Active | The question is operator-facing observability, traces, or profiling flow. |
| [plans/2026-04-20-project-constitution-and-refactor-plan.md](plans/2026-04-20-project-constitution-and-refactor-plan.md) | Reference (Tranches T0/T3 completed 2026-04-25) | The question is SSOT identity, project boundaries, or doc/release governance — the constitution itself, not its execution status. |
| [plans/tiered-kv-hicache-readmission.md](plans/tiered-kv-hicache-readmission.md) | Active | The question is staged KV readmission or remote/shared backend follow-up. |
| [plans/rust-agent-rl-single-node.md](plans/rust-agent-rl-single-node.md) | Active | The question is the Phase 6 execution path under the runtime-first rule. |
| [plans/train-runtime-architecture-v1.md](plans/train-runtime-architecture-v1.md) | Active current-architecture map | The question is today's train-side runtime/control-plane factoring. |
| [plans/train-observability-v1.md](plans/train-observability-v1.md) | Active | The question is train-side events, MLflow, OTLP, or W&B export flow. |
| [plans/train-eval-infer-dx-v1.md](plans/train-eval-infer-dx-v1.md) | Active | The question is unified operator DX across train, eval, and infer. |
| [plans/cuda-kernel-crate-extraction.md](plans/cuda-kernel-crate-extraction.md) | Reference (extraction landed; trip wires govern future splits) | The question is whether to peel another layer out of `infer` and what bar that has to clear. |
| [plans/guidellm-integration.md](plans/guidellm-integration.md) | Reference (canonical bench parameters) | The question is the exact `guidellm` parameter set or the bench wrapper contract. |

## Operator And Policy References

| Path | Role |
| --- | --- |
| [http-api.md](http-api.md) | HTTP contract and streaming behavior |
| [environment.md](environment.md) | Environment variables and runtime knobs |
| [release-checklist.md](release-checklist.md) | Release prep and artifact verification |
| [perf-and-correctness-gates.md](perf-and-correctness-gates.md) | Lightweight validation expectations by change type |
| [resources/profiling-guide.md](resources/profiling-guide.md) | GPU profiling playbook |
| [resources/metal-dflash.md](resources/metal-dflash.md) | DFlash usage runbook |
| [resources/metal-dflash-params.md](resources/metal-dflash-params.md) | DFlash CLI parameter reference |
| [resources/kv-cache-quantization.md](resources/kv-cache-quantization.md) | KV-cache quantization formats and operator-side guidance |
| [resources/infer-cuda-profiling-wrappers.md](resources/infer-cuda-profiling-wrappers.md) | `nsys` / `ncu` wrapper scripts |

## Historical Material

- `docs/experience/wins/` and `docs/experience/errors/` are the curated
  evidence log. The latest three of each are always-loaded per `AGENTS.md`;
  earlier entries are kept only when they are referenced from a KEEP file or
  document a milestone (M0–M5 tiered-kv, hybrid Qwen3.5 acceptance, train-
  side milestone snapshots, c1–c16 SGLang closure summary).
- `docs/experience/reviews/` is one Codex code-review snapshot retained as
  reference for the cuda-link audit.
- Plans / projects / research / reviews not listed in the active section
  above are not historical fallbacks: they were retired during the
  2026-04-25 truth-surface cleanup. Anything not on this index is not a
  source of truth.

## Truth-surface invariant

Per [`plans/2026-04-20-project-constitution-and-refactor-plan.md`](plans/2026-04-20-project-constitution-and-refactor-plan.md)
§2: every concern in the canonical-truth-surfaces table above has exactly
one definition. Adding a second one (a new index, a parallel `*/docs/`
tree, a sibling status matrix) is a regression and must be rejected at PR
time.
